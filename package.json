{
  "name": "bestrong",
  "version": "1.0.0",
  "description": "Gym Management Application",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "dev": "node scripts/dev.js",
    "dev:simple": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "lint": "npm run check --prefix frontend && npm run check --prefix backend",
    "lint:fix": "npm run check:fix --prefix frontend && npm run check:fix --prefix backend",
    "format": "npm run format --prefix frontend && npm run format --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  }
}
